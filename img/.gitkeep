# Placeholder to ensure img directory is tracked by git

This file intentionally left in the repository so the empty 'img' directory is included in version control. Replace or remove when you add actual image files.